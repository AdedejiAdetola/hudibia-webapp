import NotAvailable from "../../NotAvailable/NotAvailable";


const DDiseaseAlertList = () => {
    return ( 
        // if the length of the "array" is zero then display no disease alert
        <NotAvailable info="No Disease Alert"/>
     );
}
 
export default DDiseaseAlertList;